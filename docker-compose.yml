services:
  job-recommendation-agent:
    container_name: job-recommendation-agent
    build:
      context: .
      dockerfile: Dockerfile
    
    volumes:
      - ./resumes:/app/resumes:ro

    env_file:
      - .env
    
    ports:
      - "8080:8080"

    restart: "no"